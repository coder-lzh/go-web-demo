# 生产环境 - 只覆盖需要不同的配置
server:
  port: 80
  mode: release  # 生产环境关闭调试

database:
  host: 10.0.1.100
  username: prod_user
  password: Prod@2026#Secure
  database: xinxiang_prod

log:
  level: error  # 生产环境只记录错误
  file_path: /var/log/xinxiang/app.log